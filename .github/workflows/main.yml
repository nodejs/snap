name: 
on:
  pull_request:
    types: [closed]
    branches:
      - master

jobs:
  build:
    if: | 
      github.event.pull_request.merged == true
    runs-on: ubuntu-20.04
    


    steps:
      
      env:
          RELEASE_TYPE: ${{ GITHUB_HEAD_REF }}
